
import { Dimensions } from 'react-native'
const { height, width } = Dimensions.get('window');

export const COLORS = {
    bg_main: '#2d5991',
    bg_white: 'white',
    text_main: 'white'
}

export const SIZES = {
    borderRadius: 12,
    padding: 16,
    height: height,
    width: width,
}